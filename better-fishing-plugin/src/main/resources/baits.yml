baits:
  # 测试鱼饵的名称
  TestBait:
    # 测试鱼饵的显示标签
    displayName: "测试鱼饵"
    # itemsaddr插件的模型
    model: 1
    # 鱼的材质信息
    # 以下材质信息,正常情况下只会生效一个，
    # 特殊情况会要求配置两个参数，例如：药水
    item:
      material: DIAMOND
      # head-64: 头颅的base64编码
      # head-uuid:头颅主人的UUID
      # own-head: 为true，掉落钓鱼人的头颅
      # raw-material: 原始材质
      # materials: 随机材质
      # potion: 药水效果，材质为药水时生效
    # 物品发光(无属性附魔效果)
    glowing: true
    # 提升概率的鱼(提升数值为fish-weight)
    fish:
        - "TestFish1"
    # 提升概率的稀有度
    rarity:
      - "Common"
    # 鱼提升的权重(概率)
    fish-weight: 100
    # 稀有度提升的权重(概率)
    rarity-weight: 100
